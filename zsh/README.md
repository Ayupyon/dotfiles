# Zsh Configuration

Because zsh configuration file `.zshrc` has custom configuration different in machine environments, `.zshrc` showed here is only a template for configuration.

Current configuration contains:

+ network proxy config
+ custom `PATH` config
+ custom alias config
+ enable zsh history
+ config zsh plugins
+ shell prompt config
